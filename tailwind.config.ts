
<![CDATA[
import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: ["class"
...
],
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
};

export default config;
]]>


[Tool result trimmed: kept first 100 chars and last 100 chars of 2062 chars.]